{
  "name": "bun-proxy",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.2",
    "hono": "^4.6.18",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.4.2"
  }
}
